import { OpStackL2Contract } from '../../../types/opStackContracts.js';
import { ABI, FUNCTION } from '../../../types/withdrawTo.js';
import { writeOpStackL2 } from './writeOpStackL2.js';
export async function writeWithdrawERC20(client, { args: { l2Token, to, amount, minGasLimit, extraData = '0x' }, ...rest }) {
    return writeOpStackL2(client, {
        abi: ABI,
        functionName: FUNCTION,
        args: [l2Token, to, amount, minGasLimit, extraData],
        contract: OpStackL2Contract.L2StandardBridge,
        ...rest,
    });
}
//# sourceMappingURL=writeWithdrawERC20.js.map