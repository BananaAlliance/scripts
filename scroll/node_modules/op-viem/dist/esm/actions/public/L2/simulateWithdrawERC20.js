import { simulateContract } from 'viem/actions';
import { opStackL2ChainContracts } from '../../../types/opStackContracts.js';
import { ABI, FUNCTION } from '../../../types/withdrawTo.js';
export async function simulateWithdrawERC20(client, { args: { l2Token, to, amount, minGasLimit, extraData = '0x' }, ...rest }) {
    return simulateContract(client, {
        abi: ABI,
        functionName: FUNCTION,
        args: [l2Token, to, amount, minGasLimit, extraData],
        address: opStackL2ChainContracts.l2StandardBridge.address,
        ...rest,
    });
}
//# sourceMappingURL=simulateWithdrawERC20.js.map