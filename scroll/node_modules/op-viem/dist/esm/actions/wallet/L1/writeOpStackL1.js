import { writeContract } from 'viem/actions';
import { OpStackL1Contract } from '../../../types/opStackContracts.js';
import { resolveL1OpStackContractAddress, } from '../../../utils/resolveL1OpStackContractAddress.js';
export function writeOpStackL1(client, { l2Chain, contract, address, chain = client.chain, ...rest }) {
    const resolvedAddress = resolveL1OpStackContractAddress({ l2Chain, chain, contract, address });
    return writeContract(client, {
        address: resolvedAddress,
        ...rest,
    });
}
//# sourceMappingURL=writeOpStackL1.js.map