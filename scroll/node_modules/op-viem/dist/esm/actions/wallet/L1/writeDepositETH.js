import { ABI, CONTRACT, FUNCTION } from '../../../types/depositETH.js';
import { writeOpStackL1 } from './writeOpStackL1.js';
/**
 * Deposits ETH to L2 using the standard messenger contract
 * @param parameters - {@link WriteDepositETHParameters}
 * @returns A [Transaction Hash](https://viem.sh/docs/glossary/terms.html#hash). {@link WriteContractReturnType}
 */
export async function writeDepositETH(client, { args: { to, minGasLimit, extraData = '0x' }, l1StandardBridgeAddress, ...rest }) {
    return writeOpStackL1(client, {
        address: l1StandardBridgeAddress,
        abi: ABI,
        contract: CONTRACT,
        functionName: FUNCTION,
        args: [to, minGasLimit, extraData],
        ...rest,
    });
}
//# sourceMappingURL=writeDepositETH.js.map