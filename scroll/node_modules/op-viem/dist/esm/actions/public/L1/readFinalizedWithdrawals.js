import { optimismPortalABI } from '@eth-optimism/contracts-ts';
import { OpStackL1Contract } from '../../../types/opStackContracts.js';
import { readOpStackL1 } from './readOpStackL1.js';
const ABI = optimismPortalABI;
const CONTRACT = OpStackL1Contract.OptimismPortal;
const FUNCTION_NAME = 'finalizedWithdrawals';
export async function readFinalizedWithdrawals(client, { withdrawalHash, optimismPortalAddress, l2Chain, }) {
    const finalizedWithdrawal = await readOpStackL1(client, {
        contract: CONTRACT,
        abi: ABI,
        functionName: FUNCTION_NAME,
        l2Chain,
        address: optimismPortalAddress,
        args: [withdrawalHash],
        chain: client.chain,
    });
    return finalizedWithdrawal;
}
//# sourceMappingURL=readFinalizedWithdrawals.js.map