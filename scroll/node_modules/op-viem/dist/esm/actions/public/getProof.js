// NOTE(wilson): This should be suported in viem but isn't currently
export async function getProof(client, { address, storageKeys, block }) {
    return await client.request({ method: 'eth_getProof', params: [address, storageKeys, block] });
}
//# sourceMappingURL=getProof.js.map